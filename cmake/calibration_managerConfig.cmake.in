include(CMakeFindDependencyMacro)

if(USE_ZORO)
    set(OCTOPUS_API "octopus_api")
else()
    set(OCTOPUS_API "octopus_api")
endif()

find_package(${OCTOPUS_API} REQUIRED)
find_package(yaml-cpp REQUIRED)

if(NOT TARGET calibration_manager)
    include("${CMAKE_CURRENT_LIST_DIR}/calibration_managerTargets.cmake")
endif()
